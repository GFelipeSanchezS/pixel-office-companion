# Mock Event Server

Purpose

This server emits protocol v1 semantic events over WebSocket for development
and testing.

How to run

1. Install dependencies: `npm install`
2. Build: `npm run build`
3. Start: `npm start`

Development only

This server is intended for development and testing environments only.

Semantic events only

All emitted events are semantic and conform to protocol v1.
